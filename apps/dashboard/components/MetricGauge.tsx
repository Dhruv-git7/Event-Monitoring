// FILE: apps/dashboard/components/MetricGauge.tsx
import { metricColor } from '../lib/theme'
export default function MetricGauge({label,value,unit='%',sublabel=''}:{label:string;value:number;unit?:string;sublabel?:string}){const v=Math.max(0,Math.min(100,Number(value)||0));const r=42,c=2*Math.PI*r,color=metricColor(v);return <div className="panel" style={{padding:14,textAlign:'center'}}><svg width="120" height="120" viewBox="0 0 120 120"><circle cx="60" cy="60" r={r} fill="white" stroke="#e5e9ef" strokeWidth="10"/><circle cx="60" cy="60" r={r} fill="none" stroke={color} strokeWidth="10" strokeLinecap="round" strokeDasharray={c} strokeDashoffset={c-(v/100)*c} transform="rotate(-90 60 60)"/><text x="60" y="58" textAnchor="middle" fontSize="20" fontWeight="700" fill="#1a1a2e">{v.toFixed(1)}</text><text x="60" y="75" textAnchor="middle" fontSize="12" fill="#5f6b7c">{unit}</text></svg><div style={{fontWeight:600}}>{label}</div>{sublabel&&<div className="muted" style={{fontSize:12,marginTop:3}}>{sublabel}</div>}</div>}
