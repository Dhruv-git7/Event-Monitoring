// FILE: apps/dashboard/app/login/page.tsx
'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { loginUser, registerUser } from '../../lib/api'
export default function LoginPage(){const[email,setEmail]=useState('admin@platform.local'),[password,setPassword]=useState('Admin@123'),[name,setName]=useState(''),[mode,setMode]=useState<'login'|'register'>('login'),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:any){e.preventDefault();setLoading(true);setError('');try{if(mode==='login')await loginUser(email,password);else{await registerUser(email,password,name);await loginUser(email,password)}router.replace('/')}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#f4f5f7'}}><form onSubmit={submit} className="panel" style={{width:360,padding:24}}><h1 style={{margin:'0 0 4px',fontSize:22}}>Enterprise Monitoring</h1><p className="muted" style={{margin:'0 0 18px'}}>Sign in to continue</p>{error&&<div style={{border:'1px solid #ef9a9a',background:'#ffebee',color:'#c62828',padding:8,borderRadius:4,marginBottom:10}}>{error}</div>}{mode==='register'&&<input className="field" style={{width:'100%',marginBottom:10}} placeholder="Name" value={name} onChange={e=>setName(e.target.value)}/>}<input className="field" style={{width:'100%',marginBottom:10}} placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="field" style={{width:'100%',marginBottom:14}} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn btn-primary" style={{width:'100%',marginBottom:10}} disabled={loading}>{loading?'Please wait...':mode==='login'?'Login':'Create account'}</button><button type="button" className="btn" style={{width:'100%'}} onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Need an account? Register':'Have an account? Login'}</button></form></main>}
